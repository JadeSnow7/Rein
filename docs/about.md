<span id="关于本书"></span>

<span id="书的目标"></span>

# 阅读指南

打开 Rein，你会看到一份逐章长大的工作区助手。它先学会接收一句话、取回模型回答，再逐步获得读取文件、连续行动、提出修改和验证结果的能力。

我们沿着同一个项目往前走。每增加一项能力，就留下运行步骤、失败用例和能够解释的设计取舍。准备求职时，这些也是你可以拿出来展示、继续追问的具体材料。

<span id="如何阅读"></span>

## 现在从哪里开始 {#start}

默认从[第 01 章公共导读](./chapters/01.md)开始，沿主线理解并实现一次正式模型调用。遇到终端、HTTP、配置、类型或异步知识缺口时，再按需打开[阅读 0 公共导读](./readings/00.md)及对应语言补充。

| 你的路线 | 已有材料 | 下一步 |
| --- | --- | --- |
| TypeScript | [第 01 章 TS 版](./chapters/01-ts.md)，按需查阅 [阅读 0 TS 版](./readings/00-ts.md) | 用 SDK 发出 hello，逐步处理错误并验收 |
| Rust | [第 01 章 Rust 版](./chapters/01-rust.md)，按需查阅 [阅读 0 Rust 版](./readings/00-rust.md) | 用社区 SDK 发出 hello，逐步处理错误并验收 |
| 两版比较 | 第 01 章与阅读 0 的相同输入、练习编号和小节 | 按[对照索引](./readings/00.md#comparison)查看同一问题的另一种表达 |

阅读 0 的示例和练习是可选自测。完成它们不会成为进入正文的条件，也不计入主线项目交付或章节验收。

00 绪论保留原编号，等完整任务演示具备后补写。第 02–16 章、其余阅读材料和阶段汇总仍是规划，见[全书目录](./toc.md)。

<span id="两条-track"></span>

## 一套章节，两种实现 {#languages}

章节顺序按 Rein 的能力递进，两种语言使用相同编号。公共导读说明这一章要解决的问题、输入输出和验收要求；语言版带着你运行程序、阅读源码，再做练习。

侧栏每个主题只有一个入口，按选定语言进入已有版本。页面上的 TS / Rust 按钮用于切换同一主题的材料；两版都已提供时，会尽量停在对应知识点。公共导读始终可以通过正文链接打开。

如果某版还未提供，页面会明确说明。第 01 章已提供两版 SDK 调用，选择对应语言即可开始。

对照小结关注相同输入、失败阶段和行为结果。所有权、错误表达或运行时的差异，在各自语言的相关位置展开。只读一版也能沿它的路径学习，不需要先读完另一版。

## 怎样取得配套代码 {#code}

仓库的 `ts/` 与 `rust/` 分别放两种语言的实现。阅读 0 的练习位于各自的 `examples/reading-00/`，都是独立教学程序。第 01 章 SDK 调用代码在 `ts/src/` 与 `rust/src/`，跨语言运行时合同留到第 04 章推导。

跟随当前教程时，使用包含这些材料的工作版本。源码随章节累积，每章快照在相应内容完成并核验后记录。初版快照 `ch01` 保留手写 HTTP 版本；本次 SDK 教程使用 `ch01-helloworld`，具体说明见[第 01 章](./chapters/01.md#first-run)。新版标签仅本地交付时，远程克隆暂不可获取，需使用交付的本地仓库。

```bash
git tag --list
```

先查看已有快照即可。`git checkout ch01` 会切换整个仓库到历史提交，不是启动第一章，也不是切换网页语言。需要在历史版本上继续修改时，再按[阅读 0 的 Git 说明](./readings/00.md#git)保存工作并建立自己的分支。

正式书版发布时计划固定书版 tag，并对运行时合同显式版本化。当前还没有书版 tag；接口变化和适配将记录在 `MIGRATIONS.md`。

## 每章留下什么 {#evidence}

一条命令跑通之后，再追问一句，它检查到了哪里？

类型检查说明源码满足相应类型约束，测试说明列出的行为符合预期，历史回放说明已有响应怎样被解释。真实端点是否可用、回答是否解决了任务，需要各自的证据。

每章保留输入、运行方式、结果和未验证项。遇到失败，也留下报错阶段与下一步检查方向。后面复查项目时，你就能从结论找回具体过程。

<span id="参与建设"></span>

## 开放与更新 {#access}

本书目前限时免费，标注「永久免费」的内容会保持免费开放。限时免费内容后续可能调整交付方式，具体范围、时间和价格以届时公告为准。公开的 GitHub Pages 站点没有付费访问控制，详见[开放说明](./access.md)。

欢迎通过 [GitHub 仓库](https://github.com/JadeSnow7/Rein)阅读、提出问题和提交改进。新增章节与语言版本在交付后更新状态。现在可以打开[第 01 章](./chapters/01.md)沿主线开始；需要补基础时，再打开[阅读 0](./readings/00.md)。
