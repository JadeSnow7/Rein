# 章节对齐与写作审查记录

日期为 2026-09-13。本次是本地内容与导航修订，尚未提交、推送或发布。

## 修改范围

阅读 0 保留公共导读与 TS / Rust 两版，核心小节统一为运行、数据、JSON、失败、异步、源码、练习和对照。两版采用相同的三组练习编号；Rust 的所有权与借用保留在数据主题下。

第 01 章拆为公共导读 `docs/chapters/01.md` 与完整 TS 教程 `docs/chapters/01-ts.md`。公共导读说明任务、流程和验收，TS 版保留运行、源码、失败定位、回放和录制操作。Rust 正式调用尚未提供，没有创建伪实现页面。

阅读指南、目录、首页和 README 统一两种实现路线及各自进度。附录改为实现对照与深入讨论，仍标明待撰写。历史工程决策保留，在 `DECISIONS.md` 追加 D6。

导航代码由 coder 代理修改，主线程审查并进行浏览器验收。现有未提交的语言按钮改动作为起点保留，未重置用户工作。修订过程中发现并修复了公共页误禁用 Rust、直接改动 Vue 管理的 DOM、重复添加站点 base 和练习小节定位偏移的问题。

## khazix-writer 四层审读

本轮采用技能中的具体问题切入、自然讲解、失败细节、可执行动作与回环结构。产物仍是用户批准的连续教材，因此保留章节、小节、代码、表格和链接。公众号的无标题要求、固定尾部、署名、字数和情绪词数量不套用到教材；未添加虚构经历或冒用作者身份。代码、JSON、命令与 HTML 属性保留其合法标点。

### L1 硬性规则，按教材正文范围检查

扫描范围为阅读 0 的三篇、第 01 章的两篇、阅读指南和全书目录，共七篇。排除 frontmatter、代码块、行内代码与 HTML 属性后，禁用词、结构套话与空泛工具名清单零命中，禁用标点零命中。标题、步骤与对照表作为本次导航和教学要求保留，未按原技能的无标题规则宣称零命中。

### L2 风格一致性，按教材体裁审读

阅读 0 两版从把 `text` 改成数字切入，第 01 章从终端输出的来源切入。先观察结果，再解释数据、调用与失败路径；长段解释之间加入具体追问和下一步动作。结尾回到开头的输入或输出。

未为满足口语词数量、情绪标点、自嘲等公众号阈值加入无根据的作者体验。这些指标不适用于本轮教材，不能据此声称原始公众号风格检查全部通过。

### L3 内容质量

核心解释对照现有源码与测试，保持本地解析、正式调用、历史回放和真实连通性的边界。发现 TS 与 Rust 内置 `http` 场景正文不同，已在两篇练习说明中明确，下一组练习使用相同的输入表；没有把这项差异写成已经完全相同。

三组预备练习均有具体输入、操作与预期结果。第 01 章保留四组练习，明确前三项可在没有模型密钥时完成。类型检查和行为测试的区分通过可复现修改说明。未加入与任务无关的文化升华或产品推荐。

### L4 读者视角通读

按「读公共导读 → 选一版运行 → 找到失败位置 → 切到另一版同一小节」通读。修复了 Rust 所有权小节先出现 `Result` 却缺少指路说明的问题。语言版保留完整教学路径，公共页面提供明确实现入口；缺失版本不展示另一语言的操作步骤作为替代。

这是主线程的编辑判断，未作为真人读者测试或作者终审结果。

总评为四层教材适配审读完成，不作原始公众号模板逐项全通过的声明。

## 验证记录

| 检查 | 结果 |
| --- | --- |
| `npm run build` | 通过，38 个 HTML 页面 |
| 全站构建产物的本地链接与锚点扫描 | 0 处失效 |
| 原第 01 章与阅读 0 三篇的锚点兼容 | 55 个原锚点均保留 |
| 两版阅读 0 的核心 h2 顺序 | 八项一致 |
| `npm run typecheck` | TS 工程类型检查通过 |
| `npm test` | 正式调用测试 47 项通过 |
| `npm exec --workspace ts -- vitest run --config examples/reading-00/vitest.config.ts` | TS 预备练习 13 项通过 |
| `cargo test --locked --offline` | Rust 预备练习 2 项通过 |
| `cargo check --locked --offline` | Rust 编译检查通过 |
| `node --import tsx --test docs/.vitepress/theme/sourceVersionState.test.ts` | 路由与锚点策略 2 项通过 |
| 两版 success / http / json / shape CLI | 八次运行，输出与退出码均符合正文 |
| `git diff --check` | 通过 |
| frontend-design-premium 静态审计 | 0 findings，不等同完整无障碍认证 |

正式调用测试首次在沙箱内有五项因 `listen EPERM` 无法启动本地 HTTP 服务；允许回环端口后，47 项全部通过。Rust 默认离线缓存缺少依赖，按现有锁文件下载到 `/tmp/rein-alignment-cargo-home`，使用 `/tmp/rein-alignment-cargo-target` 构建，随后在这两个临时路径下完成离线测试与检查。未修改 Cargo.lock 或模型调用源码，未读取模型密钥，也未发起真实模型请求。

## 浏览器验收

使用本地 VitePress 开发预览与 Codex 内置浏览器检查了以下行为。

| 操作 | 实际观察 |
| --- | --- |
| 阅读 0 TS 的 JSON 小节切到 Rust | URL 保留 `#json`，对应标题停在阅读区顶部 |
| TS 练习 02 切到 Rust | URL 保留 `#exercise-02`，右侧目录标记同一练习 |
| 阅读 0 公共导读顶部进入 Rust | 进入 Rust 正文顶部，没有跳到篇末对照 |
| 公共页保留 Rust 偏好 | 两个按钮可用，侧栏阅读 0 指向 Rust |
| Rust 偏好下进入第 01 章 | 进入公共导读，显示并禁用「Rust 待补齐」 |
| 已保存 Rust 后显式打开 TS URL | 正文、按钮与侧栏均反映 TS |
| 用 Enter 激活公共页 TS 按钮 | 进入第 01 章 TS 版 |
| TS / Rust 页脚下一章 | TS 到正式 TS 教程；Rust 到注明缺失实现的公共导读 |
| 390 × 844 窄屏 | 文档无整页横向溢出，按钮及状态位于视口内 |
| 浏览器警告与错误日志检查 | 检查时未发现记录 |

静态构建链接检查与浏览器开发预览分别记录。没有声称线上部署、完整无障碍认证、所有浏览器兼容或模型端点可用。localStorage 异常分支已静态审查，未在浏览器中模拟禁用存储。
