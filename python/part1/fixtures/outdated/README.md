# Preview site

This small site is used by the tutorial.

Install dependencies with `npm install`.

The preview uses the development server.
npm run start

Open http://localhost:4173 after the server starts.
