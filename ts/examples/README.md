# 独立示例

标注"可独立阅读"的章节（08、09、11、12、14）在此各放一份自包含最小示例：提供自己的背景、最小接口与运行方式，不依赖主线累积到某一章的状态。

主线代码在 `../src/`。当前已添加 `reading-00/`：阅读 0 的本地响应解析练习，包含独立测试配置。运行方式见[TypeScript 预备材料](../../docs/readings/00-ts.md)，后续章节示例仍待建设。
