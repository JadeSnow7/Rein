# 复制为 .env 后填写。.env 不入库。
# tsx 不会自动加载此文件；运行命令时请显式使用 `--env-file=.env`。

# 主线使用 OpenAI 兼容接口，可指向任意兼容端点
REIN_BASE_URL=https://api.example.com/v1
REIN_API_KEY=
REIN_MODEL=

# 第 04 章计划中的对照配置，当前未被代码使用。
REIN_ALT_BASE_URL=https://api.anthropic.com
REIN_ALT_API_KEY=
REIN_ALT_MODEL=
